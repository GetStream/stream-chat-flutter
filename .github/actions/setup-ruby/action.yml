name: 'Setup Ruby'
description: 'Install Ruby and Cache Gems'
runs:
  using: "composite"
  steps:
    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: "3.3"
        bundler-cache: true
        working-directory: sample_app
