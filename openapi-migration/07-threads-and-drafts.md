# 07 — Threads & Drafts

**Goal:** Two related families that share the `Draft` model and the list controllers above them.

**Size:** 7 hand-written method(s) across 2 file(s) → 7 generated operation(s).

> Tables are generated by `openapi-migration/tool/generate_plan.py`. Edit the prose, not the tables.

## Scope

### Hand-written today

| File | Method | Returns |
| --- | --- | --- |
| `threads_api.dart` | `queryThreads` | `QueryThreadsResponse` |
| `threads_api.dart` | `getThread` | `GetThreadResponse` |
| `threads_api.dart` | `partialUpdateThread` | `UpdateThreadResponse` |
| `message_api.dart` | `createDraft` | `CreateDraftResponse` |
| `message_api.dart` | `deleteDraft` | `EmptyResponse` |
| `message_api.dart` | `getDraft` | `GetDraftResponse` |
| `message_api.dart` | `queryDrafts` | `QueryDraftsResponse` |

### Generated operations that cover it

| Verb | Path | Operation | Response |
| --- | --- | --- | --- |
| `POST` | `/api/v2/chat/channels/{type}/{id}/draft` | `createDraft` | `CreateDraftResponse` |
| `DELETE` | `/api/v2/chat/channels/{type}/{id}/draft` | `deleteDraft` | `DurationResponse` |
| `GET` | `/api/v2/chat/channels/{type}/{id}/draft` | `getDraft` | `GetDraftResponse` |
| `GET` | `/api/v2/chat/threads/{message_id}` | `getThread` | `GetThreadResponse` |
| `POST` | `/api/v2/chat/drafts/query` | `queryDrafts` | `QueryDraftsResponse` |
| `POST` | `/api/v2/chat/threads` | `queryThreads` | `QueryThreadsResponse` |
| `PATCH` | `/api/v2/chat/threads/{message_id}` | `updateThreadPartial` | `UpdateThreadPartialResponse` |

## Decisions to make

- The generated thread response embeds a full `ChannelResponse`. Decide whether the channel inside a thread adopts the generated shape now, or waits for the channels group — and write the answer down, because the two groups can otherwise disagree.

## Risks

- The four draft methods live in `message_api.dart`, not `threads_api.dart` — this group reaches into that file, and group 10 must leave those four alone.
- `Draft` and `DraftMessage` are public, persisted, and read by `StreamDraftListController` in `stream_chat_flutter_core`.

## Definition of done

- [ ] Every method above either routes through `DefaultApi` or is listed here as deliberately left
      hand-written, with the reason.
- [ ] Public methods return `Future<Result<T>>`; no `getOrThrow()` inside the SDK.
- [ ] Hand-written request/response DTOs for this group are deleted, or their retention is justified.
- [ ] `melos run analyze` clean, `melos run test:dart` green, persistence tests green if this group
      persists anything.
- [ ] `migrations/v11-migration.md`: Symbol Map rows plus a feature section for every break.
- [ ] CHANGELOG entry under `🛑️ Breaking` for each break; PR title `refactor(llc)!:`.
- [ ] Decisions recorded in this file, and the status box ticked in `README.md`.
