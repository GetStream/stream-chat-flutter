# 05 — Polls

**Goal:** First group with real domain models and persistence behind it.

**Size:** 13 hand-written method(s) across 1 file(s) → 13 generated operation(s).

> Tables are generated by `openapi-migration/tool/generate_plan.py`. Edit the prose, not the tables.

## Scope

### Hand-written today

| File | Method | Returns |
| --- | --- | --- |
| `polls_api.dart` | `createPoll` | `CreatePollResponse` |
| `polls_api.dart` | `getPoll` | `GetPollResponse` |
| `polls_api.dart` | `updatePoll` | `UpdatePollResponse` |
| `polls_api.dart` | `partialUpdatePoll` | `UpdatePollResponse` |
| `polls_api.dart` | `deletePoll` | `EmptyResponse` |
| `polls_api.dart` | `createPollOption` | `CreatePollOptionResponse` |
| `polls_api.dart` | `getPollOption` | `GetPollOptionResponse` |
| `polls_api.dart` | `updatePollOption` | `UpdatePollOptionResponse` |
| `polls_api.dart` | `deletePollOption` | `EmptyResponse` |
| `polls_api.dart` | `castPollVote` | `CastPollVoteResponse` |
| `polls_api.dart` | `removePollVote` | `RemovePollVoteResponse` |
| `polls_api.dart` | `queryPolls` | `QueryPollsResponse` |
| `polls_api.dart` | `queryPollVotes` | `QueryPollVotesResponse` |

### Generated operations that cover it

| Verb | Path | Operation | Response |
| --- | --- | --- | --- |
| `POST` | `/api/v2/chat/messages/{message_id}/polls/{poll_id}/vote` | `castPollVote` | `PollVoteResponse` |
| `POST` | `/api/v2/polls` | `createPoll` | `PollResponse` |
| `POST` | `/api/v2/polls/{poll_id}/options` | `createPollOption` | `PollOptionResponse` |
| `DELETE` | `/api/v2/polls/{poll_id}` | `deletePoll` | `DurationResponse` |
| `DELETE` | `/api/v2/polls/{poll_id}/options/{option_id}` | `deletePollOption` | `DurationResponse` |
| `DELETE` | `/api/v2/chat/messages/{message_id}/polls/{poll_id}/vote/{vote_id}` | `deletePollVote` | `PollVoteResponse` |
| `GET` | `/api/v2/polls/{poll_id}` | `getPoll` | `PollResponse` |
| `GET` | `/api/v2/polls/{poll_id}/options/{option_id}` | `getPollOption` | `PollOptionResponse` |
| `POST` | `/api/v2/polls/{poll_id}/votes` | `queryPollVotes` | `PollVotesResponse` |
| `POST` | `/api/v2/polls/query` | `queryPolls` | `QueryPollsResponse` |
| `PUT` | `/api/v2/polls` | `updatePoll` | `PollResponse` |
| `PUT` | `/api/v2/polls/{poll_id}/options` | `updatePollOption` | `PollOptionResponse` |
| `PATCH` | `/api/v2/polls/{poll_id}` | `updatePollPartial` | `PollResponse` |

## Decisions to make

- `Poll`, `PollOption` and `PollVote` are public and persisted; adopting generated shapes means touching `stream_chat_persistence` in the same PR.
- `VotingVisibility` is ours; the generated equivalent is an inline per-operation enum.

## Risks

- Vote operations live under `/chat/messages/{message_id}/polls/...`, not `/polls` — easy to miss when grepping by path.
- Poll updates also arrive over the WebSocket, so `DateTime` and `custom` handling must tolerate both encodings.
- Touching a file under `lib/src/entity/` trips `check_db_entities`, which demands a `schemaVersion` bump for any change there — including an import-only one. Do not bump it for a no-op; the guard is coarse, the schema is what matters.

## Definition of done

- [ ] Every method above either routes through `DefaultApi` or is listed here as deliberately left
      hand-written, with the reason.
- [ ] Public methods return `Future<Result<T>>`; no `getOrThrow()` inside the SDK.
- [ ] Hand-written request/response DTOs for this group are deleted, or their retention is justified.
- [ ] `melos run analyze` clean, `melos run test:dart` green, persistence tests green if this group
      persists anything.
- [ ] `migrations/v11-migration.md`: Symbol Map rows plus a feature section for every break.
- [ ] CHANGELOG entry under `🛑️ Breaking` for each break; PR title `refactor(llc)!:`.
- [ ] Decisions recorded in this file, and the status box ticked in `README.md`.
