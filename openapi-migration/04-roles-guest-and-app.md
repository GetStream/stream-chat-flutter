# 04 — Roles, Guest & App Settings

**Goal:** Sweep up the singletons — one-method families that share no state and can land in one PR.

**Size:** 4 hand-written method(s) across 3 file(s) → 5 generated operation(s).

> Tables are generated by `openapi-migration/tool/generate_plan.py`. Edit the prose, not the tables.

## Scope

### Hand-written today

| File | Method | Returns |
| --- | --- | --- |
| `roles_api.dart` | `searchRoles` | `SearchRolesResponse` |
| `guest_api.dart` | `getGuestUser` | `ConnectGuestUserResponse` |
| `general_api.dart` | `enrichUrl` | `OGAttachmentResponse` |
| `general_api.dart` | `getAppSettings` | `GetAppSettingsResponse` |

### Generated operations that cover it

| Verb | Path | Operation | Response |
| --- | --- | --- | --- |
| `POST` | `/api/v2/guest` | `createGuest` | `CreateGuestResponse` |
| `GET` | `/api/v2/app` | `getApp` | `GetApplicationResponse` |
| `GET` | `/api/v2/og` | `getOG` | `GetOGResponse` |
| `GET` | `/api/v2/longpoll` | `longPoll` | `void` |
| `GET` | `/api/v2/roles/search` | `searchRoles` | `SearchRolesResponse` |

## Decisions to make

- `AppSettings` is public and hand-shaped; the generated `AppResponseFields` is the wire shape. Keep ours unless the generated one is genuinely better.

## Risks

- `general_api.dart` is split across four groups — only `enrichUrl` and `getAppSettings` belong here. `sync` and `queryMembers` go to group 11, `searchMessages` to group 10. Do not migrate the file as a unit.

## Definition of done

- [ ] Every method above either routes through `DefaultApi` or is listed here as deliberately left
      hand-written, with the reason.
- [ ] Public methods return `Future<Result<T>>`; no `getOrThrow()` inside the SDK.
- [ ] Hand-written request/response DTOs for this group are deleted, or their retention is justified.
- [ ] `melos run analyze` clean, `melos run test:dart` green, persistence tests green if this group
      persists anything.
- [ ] `migrations/v11-migration.md`: Symbol Map rows plus a feature section for every break.
- [ ] CHANGELOG entry under `🛑️ Breaking` for each break; PR title `refactor(llc)!:`.
- [ ] Decisions recorded in this file, and the status box ticked in `README.md`.
